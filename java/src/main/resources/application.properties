# Application Configuration
quarkus.application.name=iptv-organizer-proxy
quarkus.http.port=9090

# Database Configuration
# Default to MySQL, can be changed to other databases
quarkus.datasource.db-kind=mysql
quarkus.datasource.reactive=true
quarkus.datasource.username=root
quarkus.datasource.password=pass
quarkus.datasource.reactive.url=mysql://localhost:3306/iptv_organizer

# Connection Pool Configuration (minimal for 64MB constraint)
quarkus.datasource.reactive.max-size=2
quarkus.datasource.reactive.idle-timeout=30s

# Logging
quarkus.log.level=INFO
quarkus.log.category."org.anasoid.iptvorganizer".level=DEBUG

# Native Image Configuration
quarkus.native.enable-http-url-handler=true
quarkus.native.enable-https-url-handler=true
quarkus.native.resources.includes=db/migration/*.sql

# Security
quarkus.smallrye-jwt.sign.key.location=META-INF/resources/privateKey.pem

# HTTP Client Configuration
http.client.max-pool-size=5
http.client.connect-timeout=10000
http.client.idle-timeout=60
http.client.max-redirects=5
